module NewRedirectConcern  
	# def new_redirection(user,user_id)
	#  	if user.present?
	#  		@user = User.find_by(id: user)
	#  		# flash[:errors] = "sfdsfdsfds."
	#  		return "/users/secondFa" if @user.role.eql?("Employee")
	#  		 return "/secondFA"
 #  		elsif user_id.present?
 #    		@user = User.find_by(id: user_id)
 #   			ApplicationController.new.redirect_by_role(@user)
 #  		end
	# end
end